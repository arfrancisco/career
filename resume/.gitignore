# LaTeX build artifacts
output/*.aux
output/*.log
output/*.out
output/*.fls
output/*.fdb_latexmk
output/*.synctex.gz
output/*.tex

# Keep the compiled PDFs out of git by default — they're generated by CI
# and attached as workflow artifacts. Delete these two lines if you'd
# rather commit the PDFs directly to the repo.
output/*.pdf
preview-*.png

# Editor / OS cruft
.DS_Store
*.swp
.vscode/*
!.vscode/settings.json

# Python cruft
__pycache__/
